[Journal]
# Volatile only: logs are gone on reboot. Owner accepted this so the HDD
# is not kept awake by persistent journal writes (256 MiB RAM NAS).
Storage=volatile
Compress=yes
RuntimeMaxUse=50M
RuntimeMaxFileSize=10M
MaxRetentionSec=30day
MaxFileSec=7day
